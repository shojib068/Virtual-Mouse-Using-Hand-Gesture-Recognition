# Virtual-Mouse-Using-Hand-Gesture-Recognition

dependencies:
1. Run this command in jupyter lab cell :
  import sys
  !{sys.executable} -m pip install opencv-python opencv-contrib-python
  import cv2
  cv2.__version__
2.
